scope(accum(cl_base,
cl_library),
  accum(seq(print(list_prefix(apply(apply(instantiate_if_poly(follow_if_fwd(bound_value(id('prefix +')))),
    1),
    2), if_true(true,
    list_empty,
    list_prefix(1, list_empty)))),
  map_empty),
  map_empty))